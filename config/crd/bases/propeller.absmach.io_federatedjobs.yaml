---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: federatedjobs.propeller.absmach.io
spec:
  group: propeller.absmach.io
  names:
    kind: FederatedJob
    listKind: FederatedJobList
    plural: federatedjobs
    singular: federatedjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentRound
      name: Current Round
      type: integer
    - jsonPath: .status.completedRounds
      name: Completed Rounds
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FederatedJob is the Schema for the federatedjobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              aggregator:
                description: Aggregator defines the aggregation algorithm
                properties:
                  algorithm:
                    description: Algorithm is the aggregation algorithm (e.g., "fedavg")
                    type: string
                  config:
                    description: Config contains algorithm-specific configuration
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - algorithm
                type: object
              experimentId:
                description: ExperimentID is a unique identifier for the experiment
                type: string
              hyperparams:
                description: Hyperparams are hyperparameters for the training
                x-kubernetes-preserve-unknown-fields: true
              kOfN:
                description: KOfN is the minimum number of participants required for
                  aggregation
                type: integer
              modelRef:
                description: ModelRef is the OCI reference to the initial model
                type: string
              participants:
                description: Participants is a list of proplet IDs that will participate
                  in the federated learning
                items:
                  properties:
                    namespace:
                      description: Namespace is the Kubernetes namespace where the
                        proplet is located
                      type: string
                    propletId:
                      description: PropletID is the ID of the proplet
                      type: string
                  required:
                  - propletId
                  type: object
                type: array
              rounds:
                description: Rounds defines the round configuration
                properties:
                  strategy:
                    description: Strategy is the round execution strategy (sequential
                      or parallel)
                    type: string
                  total:
                    description: Total is the total number of rounds
                    type: integer
                required:
                - total
                type: object
              taskWasmImage:
                description: TaskWasmImage is the OCI reference to the WASM image
                  for the FL client task
                type: string
              timeoutSeconds:
                description: TimeoutSeconds is the timeout for each round in seconds
                type: integer
            required:
            - aggregator
            - experimentId
            - kOfN
            - modelRef
            - participants
            - rounds
            - taskWasmImage
            - timeoutSeconds
            type: object
          status:
            properties:
              aggregatedModelRef:
                description: AggregatedModelRef is the OCI reference to the latest
                  aggregated model
                type: string
              completedRounds:
                description: CompletedRounds is the number of completed rounds
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentRound:
                description: CurrentRound is the current round number
                type: integer
              participants:
                description: Participants is the status of each participant
                items:
                  properties:
                    lastUpdate:
                      description: LastUpdate is the timestamp of the last update
                      format: date-time
                      type: string
                    propletId:
                      description: PropletID is the ID of the proplet
                      type: string
                    status:
                      description: Status is the current status of the participant
                      type: string
                  required:
                  - propletId
                  type: object
                type: array
              phase:
                description: Phase is the current phase of the job
                type: string
            type: object
        required:
        - metadata
        - spec
        - status
        type: object
    served: true
    storage: true
    subresources:
      status: {}
