---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: proplets.propeller.propeller.abstractmachines.fr
spec:
  group: propeller.propeller.abstractmachines.fr
  names:
    kind: Proplet
    listKind: PropletList
    plural: proplets
    singular: proplet
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.taskCount
      name: Tasks
      type: integer
    - jsonPath: .status.k8sStatus.readyReplicas
      name: Replicas
      type: string
    - jsonPath: .status.lastSeen
      name: Last Seen
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Proplet is the Schema for the proplets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Proplet
            properties:
              connectionConfig:
                properties:
                  channelId:
                    minLength: 1
                    type: string
                  clientId:
                    minLength: 1
                    type: string
                  clientKey:
                    minLength: 1
                    type: string
                  domainId:
                    minLength: 1
                    type: string
                  mqttAddress:
                    type: string
                  mqttQos:
                    default: 0
                    maximum: 2
                    minimum: 0
                    type: integer
                  mqttTimeout:
                    default: 30s
                    type: string
                required:
                - channelId
                - clientId
                - clientKey
                - domainId
                - mqttAddress
                type: object
              external:
                properties:
                  capabilities:
                    items:
                      type: string
                    type: array
                  deviceType:
                    description: DeviceType specifies the type of external device
                      (e.g., esp32, raspberry-pi)
                    type: string
                type: object
              k8s:
                properties:
                  image:
                    minLength: 1
                    type: string
                  logLevel:
                    default: info
                    enum:
                    - debug
                    - info
                    - warn
                    - error
                    type: string
                  replicas:
                    default: 1
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  rpc:
                    description: |-
                      RPCSpec configures the proplet JSON-RPC server, which exposes each
                      deployed function as a callable method.
                    properties:
                      enabled:
                        default: false
                        type: boolean
                      port:
                        default: 9094
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      tokenSecretRef:
                        description: |-
                          TokenSecretRef supplies the bearer token the proplet requires on
                          every RPC call. It is mandatory when Enabled is true, because the
                          server is reachable from anywhere in the cluster once exposed.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                required:
                - image
                type: object
              resources:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: ResourceList is a set of (resource name, quantity) pairs.
                type: object
              type:
                allOf:
                - enum:
                  - k8s
                  - external
                  - any
                - enum:
                  - k8s
                  - external
                type: string
            required:
            - connectionConfig
            - type
            type: object
          status:
            description: status defines the observed state of Proplet
            properties:
              availableResources:
                properties:
                  cpu:
                    description: CPU capacity (e.g., "1000m" for 1 CPU core)
                    pattern: ^([0-9]+(\.[0-9]*)?(m|))|([0-9]+m?)$
                    type: string
                  custom:
                    additionalProperties:
                      type: string
                    description: Custom resource constraints
                    type: object
                  memory:
                    description: Memory capacity (e.g., "1Gi")
                    pattern: ^[0-9]+(\.[0-9]*)?(Ki|Mi|Gi|Ti|Pi|Ei|k|M|G|T|P|E)?$
                    type: string
                type: object
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      enum:
                      - Ready
                      - Connected
                      - Healthy
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              k8sStatus:
                properties:
                  availableReplicas:
                    format: int32
                    minimum: 0
                    type: integer
                  readyReplicas:
                    format: int32
                    minimum: 0
                    type: integer
                required:
                - availableReplicas
                - readyReplicas
                type: object
              lastSeen:
                format: date-time
                type: string
              phase:
                default: Initializing
                enum:
                - Initializing
                - Running
                - Offline
                type: string
              taskCount:
                default: 0
                format: int64
                type: integer
            required:
            - phase
            - taskCount
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
