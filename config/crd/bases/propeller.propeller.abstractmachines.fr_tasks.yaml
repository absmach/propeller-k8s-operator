---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: tasks.propeller.propeller.abstractmachines.fr
spec:
  group: propeller.propeller.abstractmachines.fr
  names:
    kind: Task
    listKind: TaskList
    plural: tasks
    singular: task
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.assignedProplet
      name: Proplet
      type: string
    - jsonPath: .status.startedAt
      name: Start Time
      type: date
    - jsonPath: .status.finishedAt
      name: Duration
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Task is the Schema for the tasks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Task
            properties:
              cliArgs:
                items:
                  type: string
                type: array
              file:
                format: byte
                type: string
              imageUrl:
                type: string
              inputs:
                items:
                  type: string
                type: array
              name:
                type: string
              preferredPropletType:
                enum:
                - k8s
                - external
                - any
                type: string
              propletSelector:
                properties:
                  matchCapabilities:
                    items:
                      type: string
                    type: array
                  matchDeviceTypes:
                    items:
                      type: string
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    type: object
                  propletId:
                    type: string
                type: object
              resourceRequirements:
                properties:
                  cpu:
                    description: CPU capacity (e.g., "1000m" for 1 CPU core)
                    type: string
                  custom:
                    additionalProperties:
                      type: string
                    description: Custom resource constraints
                    type: object
                  memory:
                    description: Memory capacity (e.g., "1Gi")
                    type: string
                type: object
            required:
            - name
            - propletSelector
            - resourceRequirements
            type: object
          status:
            description: status defines the observed state of Task
            properties:
              assignedProplet:
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                format: date-time
                type: string
              error:
                type: string
              finishedAt:
                format: date-time
                type: string
              phase:
                type: string
              results:
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
            required:
            - conditions
            - createdAt
            - finishedAt
            - phase
            - startedAt
            - updatedAt
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
